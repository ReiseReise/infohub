---
title: 2026-03-04 RSS与音频异步回归加固
type: iteration-archive
status: done
version: 1.0
owner: AIReie
created: 2026-03-04
updated: 2026-03-04
tags: [archive, iteration, rss, audio, regression]
---

# 2026-03-04 RSS与音频异步回归加固

## 1. 迭代目标

1. 修复音频 `from-url` 任务阶段状态不更新问题，保证“创建即后台跑 + 阶段可观测”。
2. 固化 RSS 内容完整性与来源一致性的 P0 回归，防止再次出现“内容错乱/来源错位”。
3. 将新增 P0 回归纳入 `make gate` 全量门禁。

## 2. 变更摘要

### 2.1 音频状态回写修复

- 修改 `services/audio/app/tasks/audio_pipeline.py`：
  - `summary_result` 的 `_source_meta` 更新改为“拷贝后回写新对象”。
  - 解决 JSON 字段原地修改不触发 SQLAlchemy UPDATE 的问题。
- 修复效果：
  - `download_stage` 可从 `queued` 正确进入 `failed/finished`。
  - 前端任务详情可看到真实阶段，不再长期停留 `queued`。

### 2.2 新增 P0 回归脚本

- 新增 `qa/p0/test_rss_content_integrity.sh`：
  - 本地构造 RSS fixture，验证 `content:encoded`、`sourceName/sourceCategory`、媒体字段与详情正文完整性。
- 新增 `qa/p0/test_audio_url_async_lifecycle.sh`：
  - 验证 `from-url` 立即建任务、后台执行、失败阶段与失败码可观测。
- 更新 `scripts/qa/run-regression.sh`：
  - `--full` 模式新增执行上述 2 条 P0 用例。

## 3. 验证结果

| 检查项 | 结果 | 证据 |
|---|---|---|
| 音频状态修复 | PASS | `qa/p0/test_audio_url_async_lifecycle.sh`（6 PASS / 0 FAIL） |
| RSS 完整性回归 | PASS | `qa/p0/test_rss_content_integrity.sh`（11 PASS / 0 FAIL） |
| 全量门禁 | PASS | `qa/reports/20260304_120707-full.md` |
| 文档门禁 | PASS | `bash scripts/docs/check-docs.sh --strict`（由 `make gate` 触发） |

## 4. 下一步

1. 把 P0 音频生命周期用例扩展到“成功路径”（可控音频样本 + 完整 5 步状态流转）。
2. 为 RSS 详情补充“内容来源模式”标识（原始 RSS / 详情懒加载），增强排障可见性。
