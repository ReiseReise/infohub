# 待办清单 — 2026-03-03 音频链接增强与文档收口

> 说明：这个文件主要保留早期阶段性实施记录。当前有效优先级已迁到 `docs/03-路线图与优化计划.md` 与 `docs/02-当前状态与功能地图.md`，不要再把这里当成唯一待办入口。

---

## 任务目标（成功标准）— 2026-03-04 RSS 与音频异步回归加固
- 目标：修复 RSS/音频核心主链路可观测问题，并将其固化为 P0 自动化回归。
- 成功标准：
  - [x] 修复 `from-url` 失败后 `download_stage` 长期停留 `queued` 的问题
  - [x] 新增 `qa/p0/test_rss_content_integrity.sh` 并通过
  - [x] 新增 `qa/p0/test_audio_url_async_lifecycle.sh` 并通过
  - [x] `--full` 回归纳入 P0 套件并通过 `make gate`
  - [x] QA 索引、归档、发布说明同步更新

## 复杂度等级
- 等级：`L3`
- 判断理由：涉及 Python 异步任务状态一致性修复、P0 回归脚本新增、全量门禁链路更新与文档归档同步。

## 计划清单（可勾选）
- [x] 修复音频任务阶段状态回写（JSON 字段变更检测）
- [x] 构建 RSS fixture 回归，覆盖来源/正文/媒体字段
- [x] 构建音频 from-url 异步生命周期回归
- [x] 接入 `scripts/qa/run-regression.sh --full`
- [x] 执行 `make gate` 并登记报告

## 验证记录（DoD 证据）
- 验证方式：`bash qa/p0/test_rss_content_integrity.sh` + `bash qa/p0/test_audio_url_async_lifecycle.sh` + `make gate`
- 验证结果：通过（`qa/reports/20260304_120707-full.md`）
- 影响范围：`services/audio`、`qa/p0`、`scripts/qa`、`docs/`、`tasks/`
- 回滚点（如适用）：`v3.1.2`

## 审查结论
- 是否满足 DoD：是
- 审查备注：两条用户主诉链路已被自动化测试覆盖并纳入全量门禁，后续回归会自动拦截同类问题；并已清理 `qa_%@example.com` 测试用户与关联测试数据。

## 下一步行动
- 增加音频成功链路 P0 回归（本地可控音频样本 + 5 步状态流转断言）。

## 任务目标（成功标准）— 2026-03-04 全量恢复（并集版）
- 目标：恢复“播客专栏 + 设置中心 + 管理后台”并打通主流程（采集/翻译/音频）可用性。
- 成功标准：
  - [x] 手动采集优先级与反馈可用（不再“点了没反应”）
  - [x] RSS→评分/摘要/翻译在当前登录用户下可跑通
  - [x] 音频上传跨页面不中断，任务进度可持续查看
  - [x] 新增“播客专栏”一级导航
  - [x] 设置页补齐：通用偏好 / 阅读翻译 / 模型配置 / 集成 / 播客配额 / 管理后台
  - [x] 管理后台补齐：任务、用户、邀请码（仅 admin 可见）
  - [x] 完成 API + 前端构建验证并产出回归结果

## 复杂度等级
- 等级：`L4`
- 判断理由：涉及 Node/Python 双后端、队列调度、前端多页面重构、权限控制与全链路回归。

## 计划清单（可勾选）
- [x] 后端：采集触发高优先级 + 队列去重 + 状态接口增强
- [x] 后端：AI 定时处理按用户执行 + 单条重跑接口
- [x] 后端：配额查询/更新接口补齐
- [x] 前端：Feed/Sources 采集交互与状态提示增强
- [x] 前端：全局音频上传任务管理（跨页可见）
- [x] 前端：播客专栏页面与导航接入
- [x] 前端：设置中心六 Tab 重构并接入 admin API
- [x] 前端：浅色主题可读性与状态视觉优化
- [x] 回归：主流程 + 管理后台 + 权限边界 + 文档归档

## 验证记录（DoD 证据）
- 验证方式：`make gate`（构建 + 全量回归 + 文档门禁）+ API 冒烟
- 验证结果：通过（`qa/reports/20260304_100132-full.md`）
- 影响范围：`services/hub-engine`、`apps/web`、`docs/`、`qa/`
- 回滚点（如适用）：`v3.1.2`（当前已归档版本）

## 审查结论
- 是否满足 DoD：是
- 审查备注：主链路、设置中心、管理后台、全量门禁与文档归档均已收口；`qa/phase1` 已升级为兼容新版采集接口断言。

## 下一步行动
- 进入 P1：评分配置可视化 + 自动转写策略 + 非 RSS 信源（X/公众号）工程化接入。

## 任务目标（成功标准）— Feed × Audio 联动闭环
- 目标：在 Feed 详情页直接触发播客转写，并将音频处理状态/结果回写到条目详情。
- 成功标准：
  - [x] 新增 `POST /api/items/:id/audio-transcribe`，单条触发音频任务
  - [x] 新增 `POST /api/hooks/audio-callback`，接收音频回调并更新条目
  - [x] 音频失败也能回调，Feed 不再长期卡在 processing
  - [x] 前端支持“播客转写 + 查看音频任务”跳转
  - [x] 构建与全量门禁通过（`make gate`）

## 复杂度等级
- 等级：`L3`
- 判断理由：涉及 Node/Python 双后端联动、跨服务回调、前端交互、文档/QA 同步。

## 计划清单（可勾选）
- [x] hub-engine 增加音频触发接口与 service-to-service 调用
- [x] hub-engine 增加音频回调接口与签名校验
- [x] audio-service 兼容 UUID `article_id`，并补齐失败回调
- [x] Feed 页面增加一键转写与任务跳转
- [x] AudioStudio 支持 `taskId` 深链接打开任务
- [x] 同步文档、QA 脚本与归档

## 验证记录（DoD 证据）
- 验证方式：后端构建 + 前端构建 + Python 编译 + `make gate`
- 验证结果：通过
- 影响范围：`services/hub-engine`、`services/audio`、`apps/web`、`docs/`、`qa/`
- 回滚点（如适用）：回退至 `v3.1.1`
- 证据指针：
  - `services/hub-engine/src/routes/items.ts`
  - `services/hub-engine/src/routes/hooks.ts`
  - `services/audio/app/tasks/audio_pipeline.py`
  - `apps/web/src/pages/Feed.tsx`

## 审查结论
- 是否满足 DoD：是
- 审查备注：已完成“手动一键联动闭环”；自动转写策略（规则化触发）作为下一迭代 P1。

## 下一步行动
- 推进“按规则自动触发播客转写 + 配额策略”的自动化联动。

## 任务目标（成功标准）
- 目标：交付可用的音频链接抓取能力（含失败可排障），并补齐中文文档与功能地图。
- 成功标准：
  - [x] 音频工坊支持链接抓取并能返回来源类型/抓取策略/失败码
  - [x] 构建与全量门禁通过（`make gate`）
  - [x] 设计文档主命名中文化，能力地图可清晰标注已做/未做
  - [x] 阶段性归档、QA 索引、发布说明同步完成

## 复杂度等级
- 等级：`L3`
- 判断理由：涉及多服务链路（audio API + pipeline + web 展示 + 文档/归档/门禁脚本）和全量回归。

## 计划清单（可勾选）
- [x] 增强 `podcast_service` 链接识别与多策略兜底下载
- [x] 统一失败码并透传到任务响应
- [x] 在前端详情展示来源与失败信息
- [x] 补齐平台功能地图（含信源覆盖矩阵）
- [x] 同步归档（iteration/release）与 QA 报告索引
- [x] 跑全量门禁并记录报告路径

## 验证记录（DoD 证据）
- 验证方式：编译检查 + `make gate` + 文档门禁 + 回归报告检查
- 验证结果：通过
- 影响范围：`services/audio`、`apps/web`、`docs/`、`scripts/docs`
- 回滚点（如适用）：回退至 `v3.1.0`
- 证据指针：
  - `qa/reports/20260303_131600-full.md`
  - `docs/design/16-平台功能地图_实现状态总览.md`
  - `docs/archives/iterations/2026-03-03-音频链接抓取增强与文档中文化.md`

## 审查结论
- 是否满足 DoD：是
- 审查备注：当前主要缺口转为自动转写策略与非 RSS 信源接入。

## 下一步行动
- 推进“Feed 条目一键转写并回写状态”的端到端闭环。
